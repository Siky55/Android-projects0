<resources>
    <!-- Definice tmavého tématu aplikace -->
    <style name="Base.Theme.ToneMate" parent="Theme.AppCompat.NoActionBar">

        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>

        <!-- Zakázání reakce na nastavení uživatele -->
        <item name="android:forceDarkAllowed">false</item>
    </style>

    <!-- Použití výchozího tématu pro aplikaci -->
    <style name="Theme.ToneMate" parent="Base.Theme.ToneMate" />
</resources>
